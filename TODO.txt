# Now
- For production, remove the Strict Mode in App.jsx
- Still some issues with the status, but I don't know what's wrong
- Real-time friendslist

# Functionality
- Room authorization in backend
- File upload (or at least image upload)
- See Users Profile
- Logging service
- Delete user account (or disable)
- Automatically log in Users

# Tidiness
- Good Homepage
- Resizable
- Sort the files, espescially the frontend components
- Move package.json dependencies to devDependencies if they are supposed to be there
- Skeleton pages
- Make reusable layouts for certain page groups like Auth, Rooms, etc.

# Extra
- Better error handling
- Use JWT Tokens to access the backend, since everything in the frontend is public
- Better avatar naming, so the old avatar can be removed after changing
- Rooms will be inactive or archived instead of deleted and will be removed after 30 days.
- Searchable Dropdown for user inviting
- Invite Friends to room (easier then searching for username)
- Send an email when the user gets an invite
- Real-time room members (they can join and leave). You also need to be redirected when you are the one removed and your still in the room (with a message).

(- DevOps, zoals Docker, Prometheus & Grafana, GitHub Actions)

# Styling
- Required *'s
- Login + Register pages
- Password inputs need an eye icon for seeing password
- Dropdown menu and links 
- Navigation bar
- Modal
- UserErrorsBox
- Button (hover)
- Profile Editor
- Profile Details
- Rooms
- RoomItem
- Create Room
- ToolTip
- Invites
- InviteItem